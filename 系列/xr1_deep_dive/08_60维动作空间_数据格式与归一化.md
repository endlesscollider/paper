---
title: "60 维动作空间：双臂+底盘数据格式与归一化"
series:
  id: xr1_deep_dive
  chapter: 9
order: 9
---

# 60 维动作空间：双臂+底盘数据格式与归一化

> **前情提要**：上一章介绍了后训练的两轴对齐策略。本章深入数据层面——60 个动作维度分别代表什么、JSON 格式怎么组织、归一化怎么计算。

**知识链接**：
- 前代对照：[XR-0 数据管线](/系列/xr0_deep_dive/09_数据管线_JSON标注与相对动作计算)（32 维动作空间）
- 相关系列：[GR00T N1.7 数据格式](/系列/groot_n1d7_deep_dive/)

---

## 1. 60 维动作布局（Action Layout）

每一步动作是一个 60 维向量，按以下方式排列：

| 维度范围 | 内容 | 维数 | 说明 |
|---------|------|------|------|
| 0-2 | 左末端位置 (xyz) | 3 | 相对位移 |
| 3-5 | 左末端旋转 (axis-angle) | 3 | 轴角表示的旋转变化 |
| 6 | 左夹爪 | 1 | 开合状态 |
| 7 | (预留) | 1 | 未使用 |
| 8-10 | 右末端位置 (xyz) | 3 | 相对位移 |
| 11-13 | 右末端旋转 (axis-angle) | 3 | 轴角表示的旋转变化 |
| 14 | 右夹爪 | 1 | 开合状态 |
| 15 | (预留) | 1 | 未使用 |
| 16 | 腰部 | 1 | 腰关节角度变化 |
| 17-19 | 底盘速度 (vx, vy, vθ) | 3 | 线速度 + 角速度 |
| 20-59 | 预留 | 40 | 全零填充 |

有效维度只有 20 个（0-19），剩余 40 维是预留空间。这种设计让动作格式可扩展——未来加入更多自由度（如手指）时不需要修改模型结构。

### 1.1 为什么用轴角（Axis-Angle）表示旋转

旋转表示的常见选择：

| 表示 | 维数 | 优缺点 |
|------|------|--------|
| 欧拉角 | 3 | 有万向锁问题 |
| 四元数 | 4 | 需归一化约束 |
| 旋转矩阵 | 9 | 维数太高，冗余 |
| **轴角** | **3** | **紧凑、无约束、适合小旋转增量** |

XR-1 选择轴角的原因：
1. 只有 3 个自由度，维度紧凑
2. 作为**增量**（相对旋转）时，小角度近似下物理含义直观
3. 无需特殊的归一化层（不像四元数需要保证模为 1）
4. 和位置增量形式统一（都是 3 维向量）

### 1.2 Action Mask 的作用

`action_mask` 是一个 [30, 60] 的 0/1 张量：
- 维度 0-6, 8-14, 16-19 为 1（有效）
- 维度 7, 15, 20-59 为 0（无效/预留）

所有 loss 计算都只在 mask=1 的位置进行。这也让跨体态混合训练成为可能——单臂机器人的数据可以把另一条臂的维度 mask 掉。

## 2. 状态空间（Proprioception）

状态维度也是 60，对应 `(1, 60)` 的输入：

| 维度 | 内容 | 说明 |
|------|------|------|
| 左臂 EE pos | 3d | 末端位置 |
| 左臂 EE rotm | 9d | 旋转矩阵（展平） |
| 左臂关节角 | 最多 7d | 各关节角度 |
| 左夹爪 | 1d | 开合 |
| 右臂（同结构） | — | 镜像 |
| 腰部 | 1d | 角度 |
| 底盘 | 可选 | waist_pos |

状态输入的归一化用 `q01` 和 `q99`（1% 和 99% 分位数）做 clip + 缩放：

```python
state_normalized = (state - q01) / (q99 - q01 + eps)
```

这种分位数归一化比 min-max 更鲁棒——不受极端异常值影响。

## 3. JSON 数据格式详解

每个训练 episode 对应一个 JSON 文件：

```json
{
  "trajectory_type": "success",
  "num_frames": 1000,
  "instruction": {
    "general": [{
      "images": ["observations.ego", "observations.wrist_left", "observations.wrist_right"],
      "conversations": [
        {"from": "human", "value": "...<image>...<image>...<image>...\nGenerate robot actions for the task:\nload the washer"},
        {"from": "gpt", "value": ""}
      ]
    }]
  },
  "observations": {
    "ego": [{"path": "video.mp4", "start": 0, "crop_bbox": null}],
    "wrist_left": [...],
    "wrist_right": [...]
  },
  "proprios": {
    "left_ee_pos": [...],      // [num_frames, 3]
    "left_ee_rotm": [...],     // [num_frames, 9]
    "left_arm_joint": [...],   // [num_frames, 7]
    "left_gripper_pos": [...], // [num_frames, 1]
    "right_ee_pos": [...],
    "right_ee_rotm": [...],
    "right_arm_joint": [...],
    "right_gripper_pos": [...],
    "waist_pos": [...]         // [num_frames, 1]
  },
  "actions": {
    "left_ee_pos": [...],
    "left_ee_rotm": [...],
    "left_gripper_pos": [...],
    "right_ee_pos": [...],
    "right_ee_rotm": [...],
    "right_gripper_pos": [...],
    "waist_pos": [...],
    "base_vel": [...]
  }
}
```

### 3.1 视频 vs 逐帧

观测数据使用 **MP4 视频**而非逐帧图像——极大节约存储空间。数据加载器在训练时按需从视频中解码对应帧。

每个相机条目包含：
- `path`：视频文件路径
- `start`：起始帧偏移
- `crop_bbox`：可选裁切 `(top, left, height, width)`

### 3.2 对话模板

`conversations` 遵循标准的 human/gpt 多轮对话格式，其中：
- human 消息包含 `<image>` 占位符（按顺序消耗 `images` 列表）
- gpt 消息为空字符串（由数据加载器在运行时填充特殊 token）

## 4. 相对动作的计算

存储在 JSON `actions` 字段中的值已经是**相对量**（相对于当前帧的增量），而非绝对坐标。

对于位置：$\Delta p_t = p_{t+1} - p_t$

对于旋转（旋转矩阵 → 轴角增量）：$\Delta R_t = \text{axis\_angle}(R_t^{-1} R_{t+1})$

对于夹爪：直接取绝对值或变化量（取决于具体配置）

## 5. 归一化：Per-Step Mean/Std

XR-1 的一个独特设计：**mean 和 std 是 per-step 的**，即 [30, 60] 的张量而非全局标量。

为什么？因为动作是**累积增量序列**：
- 第 1 步的增量很小（只走了一步的位移）
- 第 30 步的"增量"实际上是从第 1 步累积到第 30 步的总位移

所以不同步骤的统计分布差异很大——第 30 步的方差远大于第 1 步。用全局 mean/std 会导致早期步骤被过度压缩，晚期步骤压缩不足。

归一化公式：

$$
\hat{a}_t = \frac{a_t - \mu_t}{\sigma_t}
$$

**这个公式在做什么**：每一步用该步专属的均值和标准差做 z-score 归一化。

::: details 📐 逐符号拆解 + 数值代入（点击展开）
**逐符号拆解**：

| 符号 | 形状 | 含义 |
|------|------|------|
| $a_t$ | [B, 60] | 第 $t$ 步的原始动作 |
| $\mu_t$ | [60] | 第 $t$ 步各维的训练集均值 |
| $\sigma_t$ | [60] | 第 $t$ 步各维的训练集标准差 |
| $\hat{a}_t$ | [B, 60] | 归一化后的动作 |

**数值代入**：以 config 中第 1 步 vs 第 30 步的 std 对比（dim 0，左 EE x 位置）：
- 第 1 步 std[0] = 0.0028（位移很小）
- 第 30 步 std[0] = 0.0271（累积位移大 ~10 倍）

如果用全局 std = 0.015（平均值）：
- 第 1 步的动作 0.001 归一化后 = 0.067（太小，梯度弱）
- 第 30 步的动作 0.03 归一化后 = 2.0（太大，梯度爆）

用 per-step std 后两步都归一化到合理范围（约 ±3 以内）。
:::

## 6. 推理时的去归一化

部署时模型输出归一化空间的动作，需要反变换回真实动作空间：

$$
a_t = \hat{a}_t \cdot \sigma_t + \mu_t
$$

**这个公式在做什么**：把模型输出的归一化动作反变换回真实物理尺度的控制指令。

::: details 📐 逐符号拆解 + 数值代入（点击展开）
**逐符号拆解**：

| 符号 | 形状 | 含义 |
|------|------|------|
| $\hat{a}_t$ | [B, 60] | 模型输出的第 $t$ 步归一化动作（约 ±3 范围） |
| $\sigma_t$ | [60] | 第 $t$ 步各维的训练集标准差（训练时计算并保存） |
| $\mu_t$ | [60] | 第 $t$ 步各维的训练集均值 |
| $a_t$ | [B, 60] | 还原后的真实动作（单位为米/弧度/m·s⁻¹） |

**数值代入**：假设模型输出第 1 步 dim 0（左 EE x 位置）的归一化值 $\hat{a}_1[0] = 1.5$，查表得 $\mu_1[0] = 0.0005$，$\sigma_1[0] = 0.0028$：

$$
a_1[0] = 1.5 \times 0.0028 + 0.0005 = 0.0042 + 0.0005 = 0.0047 \text{ m}
$$

即该步末端执行器沿 x 轴移动约 4.7 mm。

**为什么是这个形式**：这是 z-score 归一化的逆操作——训练时除以 std 减去 mean，推理时乘回 std 加回 mean，保证模型输出可以直接作为机器人控制指令。
:::

这一步在 `deploy.py` 的 Server 中完成——模型输出 [30, 60] 的归一化动作，乘以 std 加上 mean 后得到可以直接发送给机器人的控制指令。

## 7. Crop BBox：相机裁切

`crop_bbox` 允许对原始图像做裁切，格式为 `(top, left, height, width)`：
- 有些相机安装位置固定，但只有画面中央区域有用
- 裁切后再 resize 到模型输入分辨率
- 裁切区域超出画面边界时做零填充
- 设为 `null` 表示不裁切

推理时 Client API 也支持传入 `crop_bboxes`——保持训练和推理的预处理一致。

## 8. 本章小结

XR-1 的数据格式设计要点：

1. **60 维动作空间**：20 有效维 + 40 预留维，支持未来扩展
2. **轴角旋转表示**：紧凑、无约束、适合增量
3. **Per-step 归一化**：适应累积增量的不同分布
4. **视频存储**：比逐帧图像节省 10x+ 存储空间
5. **Action Mask**：让不同体态的数据无缝混合训练
6. **标准对话模板**：复用 VLM 生态的 tokenizer 和 prompt 格式

---

**下一章预告**：[Ch09 训练配置](./09_训练配置_DeepSpeed与梯度检查点) 将拆解实际训练中的 DeepSpeed 配置、优化器参数分组、梯度检查点策略。
