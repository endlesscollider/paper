# 04 章练习答案

## 1. qpos 为什么重要

图像告诉模型外部世界，但不一定精确告诉机器人每个关节和夹爪的内部状态。qpos 能帮助模型知道自己当前姿态，从而输出更准确的动作。

## 2. 动作 token 化的好坏

好处：可以复用语言模型的离散 token 生成框架。  
坏处：连续动作会被量化，可能损失精度和平滑性。

## 3. ACT 是否完全开环

不是。ACT 一次预测未来动作块，但部署时通常会在后续时间步重新观测、重新预测，并融合多个重叠预测。因此它是“短期开环 + 长期闭环重规划”。
