# 机器人学习笔记

基于 VitePress 构建的个人知识库，覆盖 Transformer、VLA、强化学习微调等方向。

## 本地部署

```bash
# 安装依赖
npm install

# 启动开发服务器（支持热更新）
npm run docs:dev
```

浏览器打开 `http://localhost:5173` 即可预览。

## 构建与预览

```bash
# 构建静态站点
npm run docs:build

# 本地预览构建产物
npm run docs:preview
```
